import React from 'react';

const ScrollbarThumbHorizontal = () => (
    <div style={{
        height: 6,
        background: "var(--primary-color)",
        borderRadius: 1,
        cursor: "pointer"
    }}></div>
)

export default ScrollbarThumbHorizontal;